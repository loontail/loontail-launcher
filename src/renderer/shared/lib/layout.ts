// The shared page content frame: a centred column capped at 1200px with a 48px
// gutter, matching the Builds list. Every route's foreground content — and the
// app bar's nav — aligns to this so left edges line up across the app. Full-bleed
// art (hero/banner backgrounds) is rendered outside it.
export const PAGE_CONTAINER = 'mx-auto w-full max-w-300 px-12';
