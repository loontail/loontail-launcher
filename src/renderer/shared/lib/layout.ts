// Shared content frame; every route's foreground aligns to this so left edges
// line up across the app. Full-bleed art is rendered outside it.
export const PAGE_CONTAINER = 'mx-auto w-full max-w-300 px-12';
